command addPath input:
  appendLine paths input
  setText input ""
command beginPathEdit edit key value:
  beginEdit edit key value
command savePathEdit list index edit:
  setLine list index (saveEdit edit)
command cancelPathEdit edit:
  cancelEdit edit
command deletePathLine list index:
  deleteLine list index
command savePaths:
  writeLines pathOutput paths
command pathListBox id list edit search:
  events:
    forLines list item index key search:
      scope key:
        when (clicked (id "edit")):
          beginPathEdit edit key item
        when (clicked (id "save")):
          when (editing edit key):
            savePathEdit list index edit
        when (clicked (id "cancel")):
          when (editing edit key):
            cancelPathEdit edit
        when (clicked (id "delete")):
          deletePathLine list index
  panel id:
    width = (prefer 800: min = 400)
    height = (fill)
    gap = 12.0
    alignItems = AlignStretch
    justifyContent = JustifyStart
    scrollX = true
    scrollY = true
    forLines list item index key search:
      scope key:
        when (editing edit key):
          row (id "edit-row"):
            width = (fill)
            height = (fit)
            gap = 12.0
            alignItems = AlignCenter
            lineInput (editInputID edit) (editInput edit):
              width = (fill)
              height = (fixed 28)
            button (id "cancel") "Cancel":
              width = (fit)
              height = (fit)
            button (id "save") "Save":
              width = (fit)
              height = (fit)
        when (not (editing edit key)):
          row (id "row"):
            width = (fill)
            height = (fit)
            gap = 12.0
            alignItems = AlignCenter
            justifyContent = JustifyCenter
            label (id "label") item:
              width = (fill)
              height = (fit)
            button (id "edit") "Edit":
              width = (fit)
              height = (fit)
            button (id "delete") "Delete":
              width = (fit)
              height = (fit)
export addPath beginPathEdit savePathEdit cancelPathEdit deletePathLine savePaths pathListBox
